{
  "name": "web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "vitest run",
    "deploy:dev": "CLOUDFLARE_ENV= astro build && CLOUDFLARE_ENV= wrangler deploy",
    "deploy:staging": "CLOUDFLARE_ENV=staging astro build && wrangler deploy --env staging",
    "deploy:production": "CLOUDFLARE_ENV=production astro build && wrangler deploy --env production"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.1.6",
    "astro": "^6.4.8",
    "db": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "pg": "^8.22.0",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@cloudflare/workers-types": "^5.20260724.1",
    "@types/pg": "^8.20.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.4",
    "wrangler": "^4.114.0"
  }
}
